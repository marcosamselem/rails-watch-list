<%= @list.name %>
<% @list.bookmarks.each do |bookmark| %>
<%= bookmark.comment  %>
<%= link_to "delete", list_bookmark_path(@list, bookmark.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
<% end %>

